# Starter pipeline
# Start with a minimal pipeline that you can customize to build and deploy your code.
# Add steps that build, run tests, deploy, and more:
# https://aka.ms/yaml

trigger:
- master

pool:
  vmImage: ubuntu-latest

stages:
- stage: Scenarios_front
  jobs:
  - job: testScenarios
    steps:
      - script: |
         npm ci
        displayName: 'Setup' 
      - script: |
          npm run cy:run
        displayName: 'Run tests' 
  